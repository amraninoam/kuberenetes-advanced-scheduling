apiVersion: v1
kind: Pod
metadata:
  name: pod80cpu
spec:
  containers:
  - name: pod80cpu
    image: vish/stress
    resources:
      requests:
        cpu: "80"
      limits:
        cpu: "80"
    args:
    - -cpus
    - "2" # try to use 2 CPU